{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "nuxt-ts",
    "lint": "eslint --ext .ts,.tsx,.js,.vue .",
    "lint:fix": "yarn lint --fix",
    "test": "yarn lint && yarn test:unit && yarn test:types",
    "test:types": "tsc -p packages/types/test",
    "test:unit": "jest"
  },
  "devDependencies": {
    "@nuxtjs/eslint-config": "^1.1.1",
    "@types/jest": "latest",
    "@typescript-eslint/eslint-plugin": "^1.13.0",
    "@typescript-eslint/parser": "^1.13.0",
    "codecov": "latest",
    "eslint": "latest",
    "jest": "latest",
    "lerna": "latest",
    "nuxt-edge": "latest",
    "typescript": "latest"
  }
}
